_base_: default.yaml

data:
  dataset: acdc
  root: /home/mamba/ML_project/Testing/Huy/med_seg/dataset/ACDC
  in_channels: 1
  num_classes: 4
  spatial_dim: 2
  img_size: [224, 224]
  class_names: [Background, RV, MYO, LV]

model:
  patch_size: 4
  spatial_dim: 2
  deep_supervision: true

training:
  batch_size: 16
  epochs: 300
  lr: 1.0e-4
